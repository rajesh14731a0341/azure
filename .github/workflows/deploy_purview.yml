name: Purview Classification Deploy

on:
  push:
    branches:
      - classifications

jobs:
  deploy:
    runs-on: self-hosted   # Linux VM runner

    defaults:
      run:
        working-directory: src/classifications

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Show Runner Info
        run: |
          echo "====================================="
          echo "Runner Information"
          echo "====================================="
          echo "Runner Name: $RUNNER_NAME"
          echo "OS: $RUNNER_OS"
          python3 --version

      - name: Step 1 - Scan Purview Catalog
        env:
          PURVIEW_CLIENT_SECRET: ${{ secrets.PURVIEW_CLIENT_SECRET }}
        run: |
          echo "====================================="
          echo "STEP 1 - Running Purview Scan"
          echo "====================================="
          python3 purview_classifier.py

      - name: Step 2 - Apply CDE Classifications
        env:
          PURVIEW_CLIENT_SECRET: ${{ secrets.PURVIEW_CLIENT_SECRET }}
        run: |
          echo "====================================="
          echo "STEP 2 - Running CDE Classification"
          echo "====================================="
          python3 purview_classifier.py rajesh_test.xlsx purview_columns_to_classify.xlsx

      - name: Completed
        run: |
          echo "====================================="
          echo "Purview Automation Completed"
          echo "====================================="